const reference = 1;

export const appMargins = {
  micro: `${reference / 4}rem`, // 4px
  milli: `${reference / 2}rem`, // 8px
  base: `${reference}rem`, // 16px
  kilo: `${reference * 2}rem`, // 32px
  mega: `${reference * 3}rem`, // 48px
  giga: `${reference * 4}rem`, // 64px
};
